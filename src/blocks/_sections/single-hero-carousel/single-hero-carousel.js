$(document).ready(() => {
    $(".single-hero-carousel").flickity({
        draggable: false,
        wrapAround: true,
        prevNextButtons: false,
        pageDots: false,
        accessibility: false
    });
});
