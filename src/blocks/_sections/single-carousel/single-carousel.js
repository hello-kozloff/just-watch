$(document).ready(() => {
    $(".single-carousel").flickity({
        cellAlign: 'center',
        draggable: false,
        prevNextButtons: true,
        pageDots: false,
        accessibility: false,
        wrapAround: true
    });
});
